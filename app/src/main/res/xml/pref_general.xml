<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="switch_reading_out_loud"
        android:title="@string/pref_titleReadingOutLoud" />

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <SwitchPreference
        android:title="@string/pref_titleShowingCodeText"
        android:key="switch_showing_code_text"
        android:defaultValue="true"/>


    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="0"
        android:entries="@array/pref_sizeListTitles"
        android:entryValues="@array/pref_sizeListValues"
        android:key="list_textSizes"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_titleChooseSize" />

</PreferenceScreen>
